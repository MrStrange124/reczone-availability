{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["bom1"],
  "functions": {
    "app.py": {
      "maxDuration": 60,
      "excludeFiles": "{tests/**,**/__pycache__/**,**/*.pyc,.pytest_cache/**,pytest.ini}"
    }
  }
}
